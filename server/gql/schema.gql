type Query {
	top100(year: Int!, day: Int!): [Song!]!
	user(id: Int!): User!
}

type Mutation {
	signup(name: String!, password: String!): User!
	login(name: String!, password: String!): User!

	favorite(songId: Int!, isFavorite: Boolean!): Song!
}

type Song {
	id: Int!
	title: String!
	artist: String!
	cover: String!
	tag: String!
	rank: Int!
	isFavorite: Boolean!
}

type User {
	id: Int!
	name: String!
	favorites: [Song!]!
}